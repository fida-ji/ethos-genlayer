# Optional frontend config. The app ships with a baked-in contract address, so
# it works with no env file. Override only if you deploy your own Ethos contract.
# On Netlify, set this as a Site environment variable instead of committing it.
VITE_CONTRACT_ADDRESS=0x3Be3c65Ef4E1755D878dD48c39D150De34a4335a
